#include <stdio.h>
int main() {
    int fact=1;
    int i,n;
    printf("enter the value ");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        fact=fact*i;
    }
    printf("factorial of %d ia %d",n,fact);
    return 0;
}
