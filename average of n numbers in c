#include<stdio.h>
int main()
{
    int n;
    int sum=0,i,avg;
    printf("Enter the range of number:");
    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        sum=sum+i;
    }
    avg=sum/n;

    printf(" %d",avg);
}
