
#include <stdio.h>

int main() {
    int n,i,sum=0;
    printf("enter a number");
    scanf("%d",&n);
    for(i=0;i<=n;i++){
        if(i%2==0){
            printf("%d,",i);
            sum=sum+i;
        }
    }
    printf("%d",sum);
    return 0;
}
